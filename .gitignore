# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
slackVenn

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Environment variables - keep templates but ignore actual secrets
.env
.env.local
env/.env
env/.env.local
env/.env.*
!env/.env.example

# Generated output files (contain user data, should not be committed)
history/*.csv
!history/.gitkeep

# Current result symlink (points to generated files)
CURRENT

# Test results and reports (generated output, different per environment)
tests/results/
!tests/results/.gitkeep

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db 